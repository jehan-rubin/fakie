language: java
sudo: false
jdk:
  - oraclejdk8
script: "mvn cobertura:cobertura"
before_install:
  - "mvn clean"
after_success:
  - bash <(curl -s https://codecov.io/bash)
cache:
  directories:
  - $HOME/.m2
